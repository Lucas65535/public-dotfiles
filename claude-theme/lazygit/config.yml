# Claude Code Theme — Lazygit configuration
#
# This is a stub that points to theme-specific configs.
# The actual theme configs are:
#   - config-dark.yml  (dark mode)
#   - config-light.yml (light mode)
#
# Your dotfiles_lazygit_config() function in 30-functions.zsh
# automatically selects the right one based on theme mode.
# Make sure your lazygit alias uses it:
#   alias lg='lazygit --use-config-file="$(dotfiles_lazygit_config)"'
